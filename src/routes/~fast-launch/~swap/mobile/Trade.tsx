import React from 'react';

import { Box } from '@chakra-ui/react';

import { TradeGraph } from '@routes/~fast-launch/components/TradeGraph';

export const Trade: React.FC = () => (
  <Box>
    <TradeGraph />
  </Box>
);
